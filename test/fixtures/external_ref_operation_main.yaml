openapi: "3.0.3"
info:
  title: External Ref Operation Main
  version: "1.0.0"
paths:
  /widgets:
    parameters:
      - name: X-Tenant
        in: header
        required: true
        schema:
          $ref: "./external_ref_nested_shared.yaml#/components/schemas/Widget"
    post:
      operationId: createWidget
      parameters:
        - name: filter
          in: query
          schema:
            $ref: "./external_ref_nested_shared.yaml#/components/schemas/Widget"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "./external_ref_nested_shared.yaml#/components/schemas/Widget"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "./external_ref_nested_shared.yaml#/components/schemas/Widget"
components: {}
