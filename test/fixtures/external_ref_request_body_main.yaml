openapi: "3.0.3"
info:
  title: External Ref Request Body Main
  version: "1.0.0"
paths: {}
components:
  requestBodies:
    CreateWidget:
      description: A widget to create
      required: true
      content:
        application/json:
          schema:
            $ref: "./external_ref_nested_shared.yaml#/components/schemas/Widget"
