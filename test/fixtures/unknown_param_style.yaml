openapi: "3.0.0"
info:
  title: Unknown Style Test
  version: 1.0.0
paths:
  /test:
    get:
      operationId: getTest
      parameters:
        - name: filter
          in: query
          style: unknownStyle
          schema:
            type: string
      responses:
        "200":
          description: OK
