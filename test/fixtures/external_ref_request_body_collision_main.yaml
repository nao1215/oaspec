openapi: "3.0.3"
info:
  title: External Ref Request Body Collision Main
  version: "1.0.0"
paths: {}
components:
  schemas:
    Widget:
      type: object
      required:
        - color
      properties:
        color:
          type: string
  requestBodies:
    CreateWidget:
      description: Creates a widget
      required: true
      content:
        application/json:
          schema:
            $ref: "./external_ref_nested_shared.yaml#/components/schemas/Widget"
